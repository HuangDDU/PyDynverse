def _method_extract_inputs(dataset, inputs):
    # 提取模型输入input
    inputs = None 
    # TODO: AnnData对象表达矩阵可以直接提取
    # inputs = dataset.X
    return dataset

def _method_extract_priors(dataset, inputs, give_prioirs=None):
    # 提取先验知识prior
    priors = None
    return priors